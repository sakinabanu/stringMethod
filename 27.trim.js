// Whitespace is all the whitespace characters (space, tab, no-break space, etc.) and all the line terminator characters (LF, CR, etc.).
//trim doesn't change the original string

const str="     javascript   "

console.log(str.trim());//javascript

const str1="    hanzala is bad boy  .   "

console.log(str1.trim());//hanzala is bad boy  .
